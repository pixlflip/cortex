# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
node_modules/
web/node_modules/
web/dist/
.venv/
venv/
.pytest_cache/

# Local config & secrets (never commit these)
cortex.yaml
cortex.admin.json
.env
*.netrc

# A runtime 'cortex init' on the bundled sample vault creates vault/.git —
# keep that out of the code repo (the sample notes themselves stay tracked).
/vault/.git/

# Vault sync internals (if a synced vault ever lives here)
.sync_*.db*
._sync_*.db*
*.~lock*

# v2 stateful data dir: the SQLite identity/gateway DB, per-vault indexes,
# and per-user vaults (design §5). Never commit any of it.
/data/

# Search index — a rebuildable SQLite FTS5 cache derived from the vault.
# Never commit this; 'cortex index' regenerates it from the notes on disk.
cortex.index.sqlite*

# OS noise
.DS_Store
Thumbs.db
